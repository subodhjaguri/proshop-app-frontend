import { StatusBar } from "react-native";
const globalStyle = {
  container: {
    width: "100%",
    height: "100%",
    alignItems: "center",
    marginTop: StatusBar.currentHeight + 10,
    flex: 1,

    alignItems: "center",
  },
  heading: {
    fontSize: 25,
    fontWeight: "bold",
    margin: 10,
  },
};

export default globalStyle;
